class HiveKeys {
  // Existing shared master keys
  static const String allFrameTypes    = 'all_frame_types';

  // Shell Type + Shell No combined master (parent-child dropdown).
  // Fetched from /shellno-by-shelltype — separate from dashboard shared masters.
  static const String shellNoByShellType = 'shell_no_by_shell_type';
}
